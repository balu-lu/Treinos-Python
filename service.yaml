apiVersion: v1
kind: Service
metadata:
  name: api-livros-service
  labels:
    app: api-livros
spec:
  type: ClusterIP
  selector:
    app: api-livros
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
